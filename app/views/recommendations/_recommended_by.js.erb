$('#recommender_' + recommended_by).html("<%= j render(partial: 'recommendations/block', locals: { products: @products, recommended_by: @recommended_by }) %>");

$('.' + recommended_by).slick({
  infinite: false,
  speed: 500,
  slidesToShow: 4,
  slidesToScroll: 4
});
