$('.rees46-recommend[data-type=' + "<%= @recommended_by %>" + ']').html("<%= j render(partial: 'recommendations/block', locals: { products: @products, recommended_by: @recommended_by, recommended_title: @recommended_title }) %>");

$('.rees46_carousel_' + '<%= @recommended_by %>').slick({
  infinite: false,
  speed: 500,
  slidesToShow: 4,
  slidesToScroll: 4
});

// Rremove slider form Home page
$('.popular-for-home .rees46_carousel_popular').slick('unslick');
