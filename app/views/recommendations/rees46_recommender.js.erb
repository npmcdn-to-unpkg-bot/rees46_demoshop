$('#recommender_' + "<%= @recommended_by %>").html("<%= j render(partial: 'recommendations/block', locals: { products: @products, recommended_by: @recommended_by }) %>");

$('.rees46_block_' + '<%= @recommended_by %>').slick({
  infinite: false,
  speed: 500,
  slidesToShow: 4,
  slidesToScroll: 4
});

// Rremove slider form Home page
$('.popular-for-home .rees46_block_popular').slick('unslick');
