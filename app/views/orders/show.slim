h2.text-center Thanks for your order 
h2.text-center Your Product Details

/ .right
/   p= item.product_id
/   p= number_to_currency(item.product.price, unit: '₽ ')
/   p= @order.id

= button_to 'Back to store', root_path

- @cart.line_items.each do |item|
  javascript:
    function afterInitRees46() {
      REES46.addReadyListener(function () {
         REES46.pushData('purchase', [{
           item_id: '#{item.product_id}',
           price: '#{number_to_currency(item.product.price, unit: '₽ ')}',
           is_available: '1',
           category_id: '#{item.product.category_id}'
         }], '#{@order.id}');
       });
    }
