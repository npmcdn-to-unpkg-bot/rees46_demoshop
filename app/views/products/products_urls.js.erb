<% if ( @recommender == 'also_bought') %>
  var recommender = 'also_bought'
  <%= render 'products/recommender.js.erb', recommender: @recommender  %>
<% end %>

<% if ( @recommender == 'interesting') %>
  var recommender = 'interesting'
  <%= render 'products/recommender.js.erb', recommender: @recommender  %>
<% end %>

<% if ( @recommender == 'similar')%>
  var recommender = 'similar'
  <%= render 'products/recommender.js.erb', recommender: @recommender  %>
<% end %>
