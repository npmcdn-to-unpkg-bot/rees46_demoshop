<% if (@recommender == 'see_also') %>
  $('#recommender_see_also').html("<%= j render(partial: 'rees46_see_also.slim', locals: { products: @products, recommender: @recommender }) %>");

  $('.delayed-sa-slick').slick({
    infinite: false,
    speed: 500,
    slidesToShow: 4,
    slidesToScroll: 4
  });
<% end %>
