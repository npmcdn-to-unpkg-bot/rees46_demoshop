$('.quantity_<%= @line_item.id %>').html('<%= @line_item.quantity %>');
$('.cart_total').html('Total <%= number_to_currency(@cart.total_price,  unit: "₽ ") %>');
$('.item_total_<%= @line_item.id %>').html('<%= number_to_currency(@line_item.total_price,  unit: "₽ ") %>')
